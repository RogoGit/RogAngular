export class Dot {
  constructor(
    public x: string,
    public y: number,
    public r: string,
    public result: boolean
  ) { }

  // constructor(values: Object = {}) {
    // Object.assign(this, values);
  // }
}
