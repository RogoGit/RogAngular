export class Dot {
  public x: string;
  public y: number;
  public r: string;

  constructor(
    public x: string,
    public y: number,
    public r: string
  ) { }

  // constructor(values: Object = {}) {
    // Object.assign(this, values);
  // }
}
